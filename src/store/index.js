import { createStore } from 'vuex'
import dayWeather from './dayWeather'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    dayWeather,
  }
})
